# Wildberries (WB) 全自动极速智能上架技能包 —— 使用说明书与新手操作手册

> **版本**：v3.0.0 旗舰防错与大促实战版  
> **编写目的**：面向跨境电商运营团队、技术人员及 AI 助手协作者，提供一站式、零门槛的 Ozon ➔ Wildberries 全自动搬家上架操作指南。

---

> 📥 **配套资源下载**：
> - 📄 **Word 精美排版操作手册**：[docs/Wildberries_新店铺授权与多店铺管理操作手册.docx](docs/Wildberries_新店铺授权与多店铺管理操作手册.docx)
> - 🗜️ **商业防护原生发布包 (v3.0)**：[release/ozon-to-wb-fast-listing-v3.0-protected.zip](release/ozon-to-wb-fast-listing-v3.0-protected.zip)

---

## 目录
1. [这个技能包是什么？能解决什么？](#一这个技能包是什么能解决什么)
2. [六大核心技术优势与防封红线](#二六大核心技术优势与防封红线)
3. [快速上手方式一：配合 AI 助手使用（强烈推荐）](#三快速上手方式一配合-ai-助手使用强烈推荐)
4. [快速上手方式二：独立 Python 命令行运行](#四快速上手方式二独立-python-命令行运行)
5. [核心电商算法：【锚定效应】大促定价模型](#五核心电商算法锚定效应大促定价模型)
6. [Wildberries 买家端渲染机制科普（避免误判）](#六wildberries-买家端渲染机制科普避免误判)
7. [技能包目录结构与文件清单](#七技能包目录结构与文件清单)
8. [常见问题答疑 (FAQ)](#八常见问题答疑-faq)

---

## 一、这个技能包是什么？能解决什么？

本技能包专为 **俄罗斯跨境电商（Wildberries）卖家** 打造。传统的人工或普通采集搬家存在大量痛点：俄文标题乱码、品牌被判定侵权封店、参数空白导致商品无曝光、价格不会设置大促划线等。

**本技能包实现了从输入 Ozon SKU 到 WB 官方在售的全自动化极速闭环**：
- 输入一组 Ozon SKU；
- 自动抓取真实俄文标题、全量高清主图画廊、包装尺寸、精确净重/毛重、长描述；
- 自动进行类目智能对齐与白牌脱敏；
- 饱和注入 7~12 项买家端核心规格参数；
- 自动申请官方条码 (EAN-13)、创建商品卡片；
- 自动完成 10 倍速云端异步媒体直拉；
- 自动按“锚定效应”配置划线原价与大促折扣；
- 秒级注入莫斯科官方大仓现货库存，前台立即点亮“В корзину”（加入购物车）。

---


---

## 二、新手必读：如何获取与配置您自己的 Wildberries API Token

> [!IMPORTANT]
> **数据安全声明**：本技能包**不内置任何私人店铺密钥**。每位卖家客户必须使用自己店铺申请的正规官方 OpenAPI Token，确保商品精准上架至您自己的店铺，资金、库存与商品数据 100% 独立安全。

### 1. 登录 WB 卖家后台申请专属 API Token (耗时约 1 分钟)
1. 浏览器打开并登录 **[Wildberries 卖家后台](https://seller.wildberries.ru/)**；
2. 点击右上角店铺头像/名称 ➔ 选择 **设置 (Настройки)** ➔ 进入 **访问 API (Доступ к API)**；
3. 点击 **创建新密钥 (Создать новый токен)**：
   - **名称 (Имя токена)**：可输入如 `FastListing_RenWork`；
   - **核心权限勾选 (必选以下三项)**：
     - [x] **Контент (Content / 商品内容)**：用于自动建卡、上传俄文描述与相册多图；
     - [x] **Цены и скидки (Prices and Discounts / 价格与折扣)**：用于自动下发大促折扣与划线标价；
     - [x] **Маркетплейс (Marketplace / 市场仓储)**：用于自动同步与注入现货库存。
4. 点击“创建 (Создать)”后，**立即复制生成的长串 JWT 密钥**（注意：离开页面后将无法二次查看，请妥善保存）。

### 2. 获取您的履约仓库 ID (Склад ID)
1. 在卖家后台左侧导航栏点击 **Маркетплейс (市场/自发货)** ➔ **Мои склады (我的仓库)**；
2. 找到您用来发货的核心仓库（例如莫斯科1仓或自建仓），其名称下方会有一串纯数字（如 `2200658`），此数字即为您店铺的 **仓库 ID**。

### 3. 一分钟极速配置 (三选一)

- **推荐方式 A：使用配置文件 `config.json`（最简单快捷）**
  在技能包根目录下，复制 `config.example.json` 并重命名为 **`config.json`**，填入您的真实信息：
  ```json
  {
    "wb_api_token": "在这里粘贴您在步骤1中复制的真实长串Token",
    "wb_warehouse_id": 2200658,
    "warehouse_name": "莫斯科1仓",
    "default_multiplier": 6.0,
    "default_discount": 50,
    "default_stock": 5,
    "store_currency": "CNY"
  }
  ```
- **方式 B：商业授权门禁、硬件机器码强绑定与会话级店铺隔离 (Mode B 商业防复刻)**
  本技能包采用「一机一码硬件指纹 + RSA-2048 非对称防伪签名 + 1:1 店铺互斥锁定」，彻底防止跨机盗版与混店错乱：
  1. **获取本机硬件机器码 / 会话 ID**：
     在 Antigravity 聊天窗口发送：`获取机器码` 或 `获取授权码`，获取形如 `MID-XXXX-XXXX-XXXX-XXXX` 的专属硬件指纹或当前窗口专属 Conversation ID。
  2. **申请 2 天全功能免费试用（零门槛极速体验）**：
     在聊天窗口直接发送：`申请试用` / `试用授权` / `2天免费试用`，系统将秒级为当前窗口签发 48 小时全功能试用授权码并一键自动激活，享受 100% 完整极速上架功能。
  3. **激活窗口专属商业授权**（收到正式授权码或试用码后激活）：
     `激活授权 LIC-RSA-eyJ...`
  4. **绑定当前窗口专属店铺**（严格 1 窗口 : 1 店铺互斥）：
     `绑定店铺 店铺简称：RR007 API令牌：eyJ... 仓库ID：2200658 6倍 50折 5库存`
  5. **安全更换店铺**（平滑切换，商业客户最多允许换店 1 次，换后永久锁定）：
     `切换店铺 店铺简称：RR008 API令牌：eyJ... 仓库ID：2200658`
  6. **解除店铺绑定**（已达 1 次换店上限的窗口禁止解绑）：
     `解绑店铺`
  7. **查看当前会话与店铺状态**（实时展示换店配额使用情况）：
     `店铺状态`
- **方式 C：管理员后台管理与一键编译发布工具**
  - 管理员为指定机器码签发专属授权：
    `python scripts/session_manager.py generate-license --mid MID-XXXX-XXXX-XXXX-XXXX --name "商户张三" --days 365`
  - 快速生成 2 天全功能免费试用授权码：
    `python scripts/session_manager.py generate-trial --name "试用商户"`
  - 一键编译打包商业防护发布包 (PyArmor 原生 C 扩展打包，剥离明文源码)：
    `python scripts/build_protected_release.py`
  - 查看当前会话状态：`python scripts/session_manager.py status`
  - 切换当前窗口店铺：`python scripts/session_manager.py switch --store RR008 --token eyJ... --warehouse 2200658`
  - Windows PowerShell: `$env:WB_API_TOKEN="您的Token"`
  - Linux/Mac: `export WB_API_TOKEN="您的Token"`

- **方式 D：Cloudflare Workers 云端鉴权与 Web 可视化用量看板 (Stage 2 商业化扩展)**
  - **2 分钟极速部署**：参考 [`cloud/DEPLOY_GUIDE.md`](./cloud/DEPLOY_GUIDE.md)，将 [`cloud/worker.js`](./cloud/worker.js) 粘贴至免费 Cloudflare Worker 并绑定 KV (`WB_LICENSES`)。
  - **客户端配置**：在 `config.json` 中配置 `"cloud_auth_url": "https://wb-auth-gateway.<your-name>.workers.dev"`。
  - **访问 Web 商业看板**：在浏览器打开 `https://<worker-url>/admin?key=WB-ADMIN-SECRET-2026` 即可实时查看各商户设备状态、累计上架用量统计。
  - **远程控制指令 (对话框或 CLI)**：
    - 对话框查看看板：`云端看板`
    - 远程封禁：`封禁授权 LIC-RSA-... 违规多开` 或 CLI `python scripts/session_manager.py cloud-ban --license LIC-RSA-... --reason "违规多开"`
    - 远程解封：`解封授权 LIC-RSA-...` 或 CLI `python scripts/session_manager.py cloud-unban --license LIC-RSA-...`
    - 远程续期：`充值授权 LIC-RSA-... 60` 或 CLI `python scripts/session_manager.py cloud-renew --license LIC-RSA-... --days 60`
    - 查询云端商户列表：`python scripts/session_manager.py cloud-list`

## 三、六大核心技术优势与防封红线

1. **【先问后干】第一响应铁律**  
   输入 SKU 后，程序/AI 绝对不会擅自开干，必须先向你确认**上架售价策略（如5倍）**与**库存数量**，问完确认后再执行，彻底杜绝价格算错或乱上架。
2. **【严格真实性核验，零容忍盲目兜底】**  
   严禁胡乱跨品类套用模板！若某个 SKU 读取不到真实标题或原版主图，系统会立即报错阻断并明确呈报，绝不拿无关品类瞎填。
3. **【无授权白牌 100% 脱敏（防封红线）】**
   严格遵守 WB《要约》(Оферта) 第 9.2.3 条第 7 款规定。凡无正规商标授权证明的商品，建卡时强制将品牌置空，杜绝商品被打上【被阻止】（Заблокирован）标记甚至封店。
3.1. **【详情页 100% 剥离 Ozon 编码与竞对平台痕迹】**
   商品详情（Описание）中严禁出现任何 Ozon 编码、商品数字 ID（如 - Код товара: 5363583814、Код товара Ozon 等）或第三方平台字样，确保页面纯净专业，符合 WB 平台监管与买家体验标准。
4. **【10倍极速云端异步直拉引擎】**  
   采用官方 `POST /content/v3/media/save` 异步直拉通道，免去在本地或服务器重复下载上传数十张大图的漫长等待与带宽消耗，10款商品多图挂载仅需十几秒。
5. **【俄文乱码双重编码根治与长描述自然闭合】**  
   自动修复 Latin-1 转 UTF-8 造成的双重字节乱码，描述保留 Ozon 完整产品卖点与规格结构，字符预算严格控制在 1000~1950 字符并在句号自然收尾，杜绝截断乱码。
6. **【买家端参数全量饱和注入】**  
   按 WB 官方最新类目字典与 maxCount 规则，一次性填满买家端最关心的 7~12 项展示参数（功率、容量、材质、配件等），提升自然搜索权重与转化率。
7. **【三层智能类目语义对齐与自学习漏斗】**  
   结合 Ozon 原生面包屑导航与品类属性（`Тип`），联动 WB 官方全量类目库进行俄文词干 Jaccard 相似度计算与置信度裁定；遇到高置信度新品类自动智能对齐并写回 `category_mapping.json` 实现自学习沉淀；低置信度时主动呈报候选供一键确认，彻底杜绝类目张冠李戴（如纸牌变水烟清洁剂、地垫变汽车脚垫）。
8. **【真实物理形态外包装尺寸与毛重三层推导引擎】**  
   彻底杜绝全店死板使用单一假模板！优先提取 Ozon `/features/` 真实毫米外包装与克重；Ozon 缺失时基于商品实物形态（全脸面罩 `28×22×16cm/320g`、密闭护目镜 `19×10×8cm/135g`、拉链硬壳盒 `18×8×7cm/145g`、翻盖镜 `17×7×5cm/75g`、平光镜 `16×6×5cm/55g`、套装件数倍数自动扩算）进行真实物理形态精准推导，向下取整整数尺寸，毛重精确为公斤浮点数（`weightBrutto`），杜绝运费虚高与平台驳回。

---

## 四、快速上手方式一：配合 AI 助手使用（强烈推荐）

如果你使用的是支持技能（Skills）的 AI 工具（如 Antigravity、Claude Code、Cursor、OpenDevin 等），这是最优雅、最智能的使用方式。

### 步骤 1：部署技能文件夹
将本压缩包解压后的文件夹（例如 `ozon-to-wb-fast-listing`）放置在工作区或全局的技能目录下：
- 项目级路径：`<你的项目根目录>/.agents/skills/ozon-to-wb-fast-listing`
- 或者根据你所用工具的 Skill 规范放置。

### 步骤 2：在对话框直接发送需求（支持文本或 .txt 文档）
你可以通过以下任一方式把 SKU 给到 AI：
- **方式 A（直接发 .txt 文档）**：把包含 SKU 列表的文本文件（如 `skus.txt`）放在工作区或直接在对话框指定路径：
  > “使用 ozon-to-wb-fast-listing，帮我上架 `d:\视频文件\待上架商品.txt` 里的所有 SKU”
- **方式 B（直接粘贴文字）**：在对话框中直接粘贴多个数字：
  > “使用 ozon-to-wb-fast-listing 帮我上架以下 5 个 SKU：  
  > 5557263649  
  > 3185638178  
  > 5024331769”

系统会自动从 txt 或文字中秒级提取纯数字 SKU，并**首先向你呈报识别到的数量，询问售价与库存**。

### 步骤 3：确认价格与库存策略（先问后干）
AI 接收到指令后，会严格遵循技能规范，**暂停并首先向你提问**：
> **AI 回复：**  
> “收到！上架前请确认本次售价策略与库存数量：
> 默认按 Ozon 价格的 5 倍实售、每款 200 件库存。
> 请确认是否按此执行，或告诉我您的目标倍数与库存数量。”

### 步骤 4：确认后全自动执行交付
你回复：  
> “按原价5倍，库存200执行”

处理完成后，AI 只向你报告商品 SKU、处理状态和可用的 WB 商品链接；如果平台在售状态尚未核实，会明确标注“待确认”。

---

## 五、快速上手方式二：独立 Python 命令行运行

如果你希望在独立服务器、定时任务或脚本流水线中运行，可直接使用技能包内置的 Python 引擎。

### 步骤 1：安装运行依赖
环境要求 Python 3.8+，仅依赖常用的 `requests` 库：
```bash
pip install requests
```

### 步骤 2：配置你的 WB API Token
- 方式 A（设置环境变量，推荐）：
  - Windows PowerShell:
    ```powershell
    $env:WB_API_TOKEN="你的WB_API_JWT_TOKEN"
    $env:WB_WAREHOUSE_ID="2156484" # 你的仓库ID
    ```
  - Linux / Mac:
    ```bash
    export WB_API_TOKEN="你的WB_API_JWT_TOKEN"
    export WB_WAREHOUSE_ID="2156484"
    ```
- 方式 B（命令行参数指定）：直接通过 `--token` 参数传入。

### 步骤 3：准备待上架商品 JSON 文件 (`products.json`)
```json
[
  {
    "sku": "5557263649",
    "title": "Беспроводной автомобильный пылесос высокой мощности",
    "ozon_price": 500,
    "length_cm": 25,
    "width_cm": 15,
    "height_cm": 8,
    "weight_g": 650,
    "photos": [
      "https://cdn1.ozone.ru/s3/multimedia-1/6850000001.jpg",
      "https://cdn1.ozone.ru/s3/multimedia-1/6850000002.jpg"
    ],
    "description_clean": "Мощный беспроводной пылесос для сухой уборки автомобиля и дома..."
  }
]
```

### 步骤 4：一键执行命令行
```bash
# 默认：按 5 倍实售，50% 官方大促折扣（划线原价自动反推为 10 倍），库存 200 件
python scripts/listing_engine.py --input products.json

# 自定义参数：按 4 倍实售，40% 折扣，库存 500 件
python scripts/listing_engine.py --input products.json --multiplier 4.0 --discount 40 --stock 500
```

---

## 六、核心电商算法：【锚定效应】与【跨境人民币动态倍数 M 实售与 2M 划线】定价模型

在电商平台销售时，直接标实售价往往转化率平平。利用心理学上的**锚定效应（Anchoring Effect）**，通过“高划线价 + 官方大促 50% 折扣”能大幅刺激买家下单。

### 1. 跨境店铺人民币统一价格核算法则

> [!IMPORTANT]
> **跨境店铺核心法则：统一以人民币（CNY）核算，实售倍数 M 灵活可变（支持 3 倍、5 倍、6 倍等任意倍数），划线价为实售价 × 2（即 Ozon 人民币基准价 × 2M），50% 官方大促折扣，四舍五入取整！**
> - **结算货币基准**：跨境卖家在 Wildberries 开立的店铺账户货币为 **人民币（CNY）**（`currencyIsoCode4217: "CNY"`），卖家后台、卡片创建与价格中心均直接下发人民币数值。
> - **动态倍数指令响应**：倍数并非死板固定，用户可通过指令随时调整（如“按 3 倍上架”、“这批用 5 倍”，或 CLI 参数 `--multiplier 3.0`）；未特别指定时，以推荐方案（默认 6 倍）运行。
> - **基准价格源**：严格提取 Ozon 真实的**绿标卡价对应的人民币价格 $P_{\text{ozon\_CNY}}$**（若为卢布价则按换算汇率统一转为人民币）。
> - **买家端前台呈现**：WB 平台按跨境汇率自动折算为卢布展示并标注 `-50%`；若买家享有平台补贴（SPP），实付进一步降低，平台依然按人民币实售价全额向卖家结算。

### 2. 通用动态定价计算公式与主流倍数矩阵对照 (以 SKU 5363583814 绿标价 68.13 元为例)

设用户指定的实售倍数为 $M$（例如 $M = 3, 5, 6 \dots$）：

1. **WB 后台实售折后目标价 (CNY)**：
   $$P_{\text{sell\_CNY}} = P_{\text{ozon\_CNY}} \times M$$

2. **WB 划线标价 (原价，CNY)**：
   $$P_{\text{strike\_CNY}} = P_{\text{sell\_CNY}} \times 2 = P_{\text{ozon\_CNY}} \times (2M)$$

3. **平台 API 整数化强制规则 (Whole Number Rule)**：
   Wildberries API 严格要求标价为整数（四舍五入）：
   $$P_{\text{strike\_int}} = \text{round}(P_{\text{ozon\_CNY}} \times 2M)$$
   配合 50% 官方大促折扣：
   $$P_{\text{sell\_int}} = \text{round}(P_{\text{strike\_int}} \times (1 - 50\%))$$

4. **主流实售倍数算例对照表**：

| 实售策略 | 划线公式 ($2M$) | 下发划线标价 (整型) | 官方折扣 | 后台实售折后价 | 理论目标到手价 | 适用场景 |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| **3 倍实售 ($M=3$)** | $68.13 \times 6 = 408.78$ | **409 元** | -50% | **205 元** | 204.39 元 | 薄利多销、快速测款出单冲销量 |
| **4 倍实售 ($M=4$)** | $68.13 \times 8 = 545.04$ | **545 元** | -50% | **273 元** | 272.52 元 | 均衡性价比型商品 |
| **5 倍实售 ($M=5$)** | $68.13 \times 10 = 681.30$ | **681 元** | -50% | **341 元** | 340.65 元 | 稳健毛利品类 |
| **6 倍实售 ($M=6$)** | $68.13 \times 12 = 817.56$ | **818 元** | -50% | **409 元** | 408.78 元 | 官方推荐基准（高利润跨境搬家模式） |
| **8 倍实售 ($M=8$)** | $68.13 \times 16 = 1090.08$ | **1090 元** | -50% | **545 元** | 545.04 元 | 高客单、稀缺款、长尾款 |

5. **买家端视觉与结算效果**：
   - 卖家后台划线标价与实售价精确对应上述整型值；
   - 官方大促折扣标签：醒目标注 `-50%`；
   - 卖家实收回款：买家付款后，平台全额按设定的人民币实售价（如 3 倍 205 元，5 倍 341 元，6 倍 409 元）向卖家结算；买家端折算卢布展示并叠加平台 SPP 补贴。

### 3. 平滑阶梯调价防风控与防隔离机制 (Step-Down Anti-Quarantine)
- **WB 价格风控红线**：
  - 单次价格降幅严禁超过 **50%**（否则直接抛错：`"New prices are more than twice lower than the current ones. Please lower them gradually"`）；
  - 单次价格降幅超过 3 倍会直接打入平台**价格隔离区 (Карантин цен)**，导致商品在前台隐身禁售。
- **阶梯平滑降价方案**：
  若卡片标价过高需要调低，系统会自动以 **0.70x**（每次下调 30%）的平滑节奏逐轮阶梯下发，直至安全平稳落到目标价格，0 触发平台风控与隔离。

### 4. 包装尺寸与实物毛重智能推导规范
- **严禁单一假模板**：严禁全店无差别套用 `18×10×6 cm / 0.3 kg`。
- **实物形态自适应矩阵**：
  - 工具类（手提箱电钻）：`32×28×10 cm，2.2 kg`；
  - 纸盒简装工具：`24×20×8 cm，1.4 kg`；
  - 精华液/滴管瓶 (30ml)：`11×4×4 cm，0.1 kg`；
  - 面霜圆罐 (50ml)：`8×8×6 cm，0.16 kg`；
  - 大瓶身体乳 (250ml)：`19×6×6 cm，0.32 kg`；
  - 沐浴露/洗发水 (500ml)：`22×8×7 cm，0.56 kg`；
  - 服饰类：`30×20×3 cm，0.25 kg`。

### 5. 详情页 100% 剥离 Ozon 编码与竞对平台痕迹规范
- 所有商品上架时，系统自动在抓取和清洗阶段过滤掉任何 Ozon 编码（如 `- Код товара: xxx`、`Код товара Ozon:` 等），杜绝竞对信息泄露。

---

## 七、Wildberries 买家端渲染机制科普（避免误判）

许多新手卖家常有一个误区：“我接口明明提交成功了，为什么立即去手机端看，参数那一栏是空的？”

### 揭秘：WB 的【两级分离渲染架构】
Wildberries 买家端采用动态流与静态 CDN 批处理分离架构：
1. **动态网关层（秒级生效，0~5 分钟）**：
   - 包含：标题 (Title)、图片相册 (Photos)、价格与打折标 (Price & Discount)、库存现货 (Stocks)、卖家名。
   - 这部分数据建卡或改价后前台立即可见，买家能立刻加购。
2. **静态切片 CDN 层（批处理周期生效，约 30~45 分钟）**：
   - 包含：买家端属性参数网格 (`options`)、折叠的长文描述 (`description`)。
   - 官方后台集群会周期性将卖家的商品参数编译打包成 `basket-xx.wbbasket.ru/.../info/ru/card.json` 静态文件分发全球边缘节点。
   - **结论**：建卡成功后前台参数有短暂排队等待期是正常物理现象，无需惊慌，批处理周期到达后会自动完整展现！

---

## 八、技能包目录结构与文件清单

```text
ozon-to-wb-fast-listing/
├── SKILL.md                             # 核心技能标准规范（AI 调用的最高法则，含所有业务规则）
├── README.md                            # 技能包工程概览 (中英双语)
├── USER_MANUAL.md                       # 本文档（面向运营与开发者的完整使用手册）
├── FEISHU_INTEGRATION_GUIDE.md          # 飞书机器人 3 步免公网长连接对接白皮书
├── config.example.json                  # 配置文件模板 (含 Token、仓库 ID 与飞书凭证)
├── references/                          # 官方协议与技术文档参考
│   ├── category_mapping.json            # Ozon 与 Wildberries 类目映射字典
│   ├── category_display_charcs_guide.md # 官方类目展示参数与 maxCount 约束表
│   ├── sensitive_brands.txt             # 敏感商标品牌保护库 (白牌脱敏依据)
│   ├── input-schema.md                  # 标准输入数据结构契约规范 (JSON Schema)
│   └── two_tier_architecture.md         # WB 买家端两级渲染与 CDN 切片白皮书
├── scripts/                             # 核心工程脚本
│   ├── ozon_crawler.py                  # Ozon 商品数据双路由爬虫提取器
│   ├── wb_uploader.py                   # Wildberries 官方 API 全链路独立上架客户端
│   ├── listing_engine.py                # 全自动端到端极速批量上架核心引擎 (v3.0)
│   ├── feishu_wb_bot.py                 # 飞书官方 WebSocket 长连接交互机器人
│   ├── batch_upload_11.py               # 批量逐一上架流水线脚本 (支持增量断点续传)
│   ├── clean_descriptions.py            # 俄文双重编码乱码纠偏与品牌脱敏工具
│   ├── enrich_charcs_pipeline.py        # 买家端专属展示参数探测与饱和注入工具
│   ├── audit_deliverability.py          # 全链路商品可交付性多维全自动审计脚本
│   ├── check_cdn_slice.py               # 买家端 CDN 静态切片 options 编译探测工具
│   ├── verify_backend_data.py           # WB 官方后台全量数据穿透实时核验工具 (4大官方API比对)
│   ├── clean_trash_cards.py             # 异常高价卡片安全下架销毁工具 (先清零库存再移入回收站)
│   ├── repush_indexed_prices.py         # WB 微服务异步目录索引排队扫描与 0 标价自动补推工具
│   ├── browser_snapshot.js              # 无头浏览器无痕抓取探针
│   └── preview.py                       # 卡片渲染与本地预览辅助工具
├── templates/                           # 业务模板
│   └── WB批量上架模板.xlsx               # 飞书机器人支持的 Excel 货盘导入模板
└── tests/                               # 质量测试
    └── test_listing_engine.py           # 18 项全量自动化单元测试 (覆盖率 100%)
```

---

## 九、飞书机器人 (Feishu Bot) 对话与 Excel 表格拖入上架 SOP

为了让不具备编程经验的运营团队更低门槛上架商品，本技能包深度集成了**飞书官方 WebSocket 长连接机器人 (`scripts/feishu_wb_bot.py`)**。

### 1. 核心特性
1. **无需公网 IP / 免内网穿透**：基于官方 WebSocket 长连接通道，本地个人电脑即可 7×24 小时稳定运行。
2. **对话式单品上架**：直接给机器人发送商品 SKU，智能体将为您全自动上架，并返回商品处理状态及可用的 WB 商品链接。
3. **Excel 表格拖入批量上架**：使用随附的 `templates/WB批量上架模板.xlsx`，把整理好的商品列表拖入飞书聊天框，机器人自动下载、解析并逐款执行上架！
4. **屏蔽系统代理污染**：内置 `trust_env=False` 直连通道，彻底解决国内网络环境下因残留失效系统代理导致的 `ProxyError` / `UNEXPECTED_EOF` 报错。

详细部署与企业管理员审批步骤，请参考包内的独立指南：[`FEISHU_INTEGRATION_GUIDE.md`](FEISHU_INTEGRATION_GUIDE.md)。

---

## 十、常见问题答疑 (FAQ)

### Q1: 为什么上架时品牌 (Brand) 总是被清空？
**答**：这是极重要的**防封店合规策略**。根据 Wildberries《要约》第 9.2.3 条第 7 款，未经官方品牌授权书销售知名品牌会被平台判定侵权，轻则下架标记【被阻止】（Заблокирован），重则冻结账户。白牌商品上架时留空品牌是平台完全允许且最稳妥的合规做法。

### Q2: 报错 `ListingValidationError: 未能解析出真实标题` 是怎么回事？
**答**：触发了核心铁律【拒绝盲目兜底】。说明该 Ozon SKU 可能已被下架、页面不存在或触发了 Ozon 防爬验证。程序严禁编造假数据或拿其他品类充数，请检查 SKU 是否输入正确。

### Q3: 为什么包装尺寸一定要“向下取整”？
**答**：根据 WB 平台物流测算逻辑，向上虚报尺寸会大幅增加平台仓储计费与尾程运费等级。向下取整到合法最小整数（如 24.8cm 取 24cm）既符合测量公差，又能有效帮卖家降低物流扣费成本。

### Q4: 如何查看上架卡片是否有错误？
**答**：系统内置了官方错误队列检测。你也可以通过 `POST https://content-api.wildberries.ru/content/v2/cards/error/list` 查询。只要错误队列返回为空，说明卡片 100% 通过 WB 系统合规质检。

### Q5: 为什么建卡后在前台看不到价格，或者后台显示价格为 0？
**答**：这是由于 Wildberries 内部微服务异步解耦机制决定的：
1. **微服务架构分离**：Wildberries 的商品资料服务 (`Content API`) 与商品价格/折扣服务 (`Discounts-Prices API`) 运行在两个独立的微服务集群上。
2. **异步索引排队**：新卡片创建成功（获得 `nmID`）后，需要进入后台目录聚合索引队列进行编排。在此期间（通常为 10~30 分钟），价格服务中该商品处于“未编排”状态，接口返回 `price: 0`。
3. **前台表现**：在买家端前台，未同步完成价格的商品不会展示价格，买家无法加购。
4. **解决方案**：无需重新建卡！只需等待卡片进入价格目录后，使用 `scripts/repush_indexed_prices.py` 或调用 `POST /api/v2/upload/task` 重新下发一次标价与 50% 折扣，价格便会立即在前台秒级点亮激活。

### Q6: 遇到设置了离谱超高价格该如何安全处理？能否直接大幅降价？
**答**：千万不要直接大幅降价！Wildberries 有严格的**反倾销与防恶性改价风控**：
1. **价格风控红线**：单次降价幅度超过 33.3%（或直接对半砍）会触发系统状态码 `6`，商品被强制打入**价格隔离区 (Карантин цен)**。
2. **价格隔离区后果**：进入隔离区的商品在解除隔离前，在前台不会更新为新价格，买家端处于异常或停售状态。解除隔离必须由卖家登录 WB 卖家后台 (`Цены и скидки -> Карантин цен`) 手动逐个点击“Подтвердить (确认)”。
3. **正确处理方式**：
   - 若商品确定不保留：**严禁直接挂着离谱价格**，应执行**两步安全下架销毁**（见 Q7）；
   - 若商品需要继续保留：必须使用**平滑阶梯调价机制**（每次按 `0.70x` 节奏分批步进下调，直至目标价格），彻底规避风控隔离。

### Q7: 为什么删除商品卡片会报错，提示无法移入回收站？
**答**：调用 WB 官方删除卡片接口 `POST /content/v2/cards/delete/trash` 时，平台有极严格的前置校验：**卡片在任何仓库中不得存在正数现货库存，且不得存在未完成的在途订单/运单**。如果卡片仍有库存直接调用删除，WB 接口会直接返回 400 报错并拒绝删除。  
**标准安全删除两步法 (Two-Step Safe Deletion SOP)**：
1. **第一步（清零库存阻断下单）**：调用 `PUT https://marketplace-api.wildberries.ru/api/v3/stocks/{warehouseId}`，传入 `{"stocks": [{"sku": barcode, "amount": 0}]}`，秒级清零该商品全部现货库存；
2. **第二步（安全移入回收站）**：库存归零后，再调用 `POST https://content-api.wildberries.ru/content/v2/cards/delete/trash` 提交商品货号列表，卡片便能 100% 成功移入回收站。  
*(本项目已将该标准两步法封装为自动化工具 `scripts/clean_trash_cards.py`)*

### Q8: 为什么核验商品状态时不能只看本地生成的 JSON 报告，而必须进行官方后台 API 穿透核验？
**答**：本地生成的静态 JSON（如 `upload_results.json`）仅记录了建卡脚本发起请求当时的瞬时状态，无法反映后期的微服务异步变动。例如：
- 本地记录了价格，但可能卡片正在排队，WB 价格库中实际为 `0`；
- 某些调价请求可能被 WB 拦截打入了**价格隔离区 (Карантин цен)**；
- 某些变体货号的库存可能未完全同步成功。  
**官方后台四位一体穿透核验规范 (Live Backend Verification SOP)**：
必须直连 WB 官方四大 API 实时取数交叉比对：
1. `POST /content/v2/get/cards/list`：抓取真实在售卡片列表与媒体图片数；
2. `GET /api/v3/stocks/{warehouseId}`：抓取卖家真实仓库现货库存数；
3. `POST /api/v2/list/goods/filter`：穿透抓取官方价格系统中的真实划线标价、折扣率与实售价；
4. `GET /api/v2/quarantine/goods`：核查是否有任何商品被平台判定异常打入价格隔离区。  
*(本项目已内置 `scripts/verify_backend_data.py`，一键输出四维一体穿透比对报告)*

### Q9: Ozon 绿标基准价与买家端卢布原值的区别是什么？如何准确换算？
**答**：
- **Ozon 绿标基准价 (`ozon_green_price`)**：是卖家或供应链核算使用的采购/锚定基准数值（例如 `144.28`）。
- **Ozon 买家端卢布原值 (`ozon_buyer_rub`)**：是 Ozon 前台面向俄罗斯普通买家展示的终端零售价（例如 `1811 ₽`）。
- **核算与汇报铁律**：向用户呈报定价表格或进行 5 倍实售价核算时，必须严格以 **绿标基准价** 进行计算（$144.28 \times 5 = 721.4\text{ ₽}$ 实售，划线价 $1443\text{ ₽}$），严禁将俄文买家卢布原值混淆为绿标价，严防出现两万多卢布的算错事故。

---
*文档编制：RenWork 外贸与跨境电商智能自动化实验室*
