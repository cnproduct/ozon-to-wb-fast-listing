# 2026-09-18 免费 2 天试用发布检查

## 行为

- 新用户、旧用户（包括以前领取过旧版试用的用户）输入「申请试用」，使用真实会话 ID 领取本轮 48 小时授权。
- 同一会话在本轮重复申请不续期；有效正式授权不会被试用覆盖。
- 网络中断后重试同一会话，会取回原试用码和原到期时间，不再重复签发。
- 本地脚本、Agent Skill、飞书文本入口和云端免费套餐均已更新。云端以会话 ID 去重；跨不同会话的同一 WB 店铺仍需在店铺绑定环节做统一身份校验。

## 上线前必须完成

1. 对照正在运行的 Worker 代码与此仓库的 `cloud/worker.js`，合并线上独有变动。健康接口成功不代表线上代码与本地相同。
2. 在 Cloudflare Secret 中配置并轮换 `RSA_SIGNING_KEY`、`ADMIN_SECRET`，若使用支付宝还须检查 `ALIPAY_PRIVATE_KEY`。旧版公开代码包含私钥和管理口令；历史授权兼容和迁移应在轮换前核查。不可把密钥写入提交、日志或发布包。
3. 用隔离测试 KV 和测试会话验证首领、重复领、48 小时到期、已有付费授权、旧版试用、真实店铺绑定与飞书收发；并发跨边缘重复领取须采用强一致存储。Workers KV 是最终一致的，不提供原子“只领一次”保证。
4. 生成新版受保护 Windows 发行包并验证其运行时、技能指令、依赖、密钥扫描；旧版压缩包不会自动更新。对已安装用户发升级说明，确认他们实际安装了新版脚本及技能。
5. 审核后再推送 GitHub、部署 Worker 和替换下载包；逐一验证公开路径、用户申请及真实授权状态。

## 旧授权统一重新签发

用户已同意统一重新签发。执行前导出完整线上授权记录并保留可回滚备份；逐条区分正式授权、旧试用、封禁或过期授权。正式授权须核对真实 WB 店铺 ID、客户归属、原到期时间和支付/代理记录，重新签发后保留原剩余有效期，不把正式授权降级为试用。旧试用用户可另领本轮 48 小时试用；封禁或身份不明的记录先人工核实。仓库现有授权记录通常只有店铺名称与机器/会话 ID，不能据此推断真实 WB 店铺 ID，因此缺少 ID 的记录不可自动迁移。新旧码的切换与私钥轮换应在真实客户端、在线验签和店铺隔离均通过后进行。

## 2026-09-18 线上只读核对

- Cloudflare 账号中 `wb-auth-gateway` 的生产部署版本为 `6933fdd3`，已绑定 `WB_LICENSES`；当前线上代码与本仓库草稿不同。
- KV 中发现两条正式授权记录，原到期时间均为 `2026-10-17 23:59:59`，有机器 ID，但均没有真实 WB 店铺 ID 字段。需要逐条确认对应的店铺 ID，之后才能安全重新签发。
- 线上 Worker 代码仍嵌入签名私钥及支付私钥，配置页仅能看到 `ADMIN_SECRET` 和 `ALIPAY_APP_ID` 变量，未见 `RSA_SIGNING_KEY` Secret。先备份和核对旧码，再轮换私钥及管理口令；不要把线上代码原样复制到 PR、日志或本文件。
- 此次仅进行了只读检查，尚未签发新码、替换旧码或部署 Worker。

本地验证命令：`python -m unittest discover -s tests -p 'test_*.py' -q` 与 `node tests/test_trial_worker.mjs`。
